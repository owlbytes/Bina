<%= form_for(@torso) do |f| %>
  <% if @torso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@torso.errors.count, "error") %> prohibited this torso from being saved:</h2>

      <ul>
      <% @torso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
