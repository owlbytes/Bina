<div class="container">
  <div class="sixteen columns" id="work">
    <h3>Torsos</h3>
    <% @torsos.each do |torso| %>
      <%= link_to image_tag(torso.image_url), torso %>
    <% end %>
  </div>
</div>
