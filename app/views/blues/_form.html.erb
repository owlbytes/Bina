<%= form_for(@blue) do |f| %>
  <% if @blue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blue.errors.count, "error") %> prohibited this blue from being saved:</h2>

      <ul>
      <% @blue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
