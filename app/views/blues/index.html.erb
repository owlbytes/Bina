<div class="container">
  <div class="sixteen columns" id="work">
    <h3>Blues</h3>
    <% @blues.each do |blue| %>
      <%= link_to image_tag(blue.image_url), blue %>
    <% end %>
  </div>
</div>
