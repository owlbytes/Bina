<div class="container">
  <div class="sixteen columns" id="work">
    <h3>Figures</h3>
    <% @figures.each do |figure| %>
      <%= link_to image_tag(figure.image_url), figure %>
    <% end %>
  </div>
</div>
