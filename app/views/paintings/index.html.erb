<div class="container">
  <div class="sixteen columns" id="work">
    <h3>Abstracts</h3>
    <% @paintings.each do |painting| %>
      <%= link_to image_tag(painting.image_url), painting %>
    <% end %>
  </div>
</div>
